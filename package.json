{
  "name": "a11y-audit-cli",
  "version": "0.1.0",
  "description": "CLI tool that runs accessibility checks on URLs and outputs structured reports with fix suggestions",
  "main": "dist/index.js",
  "bin": {
    "a11y-audit": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/cli.ts",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "axe-core": "^4.8.2",
    "chalk": "^4.1.2",
    "commander": "^11.1.0",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.8",
    "@types/node": "^20.10.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
